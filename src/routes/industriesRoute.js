// const express = require("express");
// const industries_route = express.Router();

// industries_route.use(express.static("public"));

// // Passing the json incoming request
// industries_route.use(express.json());
// industries_route.use(express.urlencoded({ extended:true }));

// const jsonData = require("../data/industriesWeServe");

// industries_route.get('/indusriesWeServe', function(req, res){
//     res.send(jsonData);
// });

// module.exports = industries_route;